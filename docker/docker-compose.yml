services:
  postgres:
    image: postgres:17

    environment:
      POSTGRES_DB: flashseat
      POSTGRES_USER: flashseat
      POSTGRES_PASSWORD: flashseat_dev123

    ports:
      - "5432:5432"

    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: